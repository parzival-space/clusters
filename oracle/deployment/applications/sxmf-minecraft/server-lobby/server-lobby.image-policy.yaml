apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: server-lobby
spec:
  imageRepositoryRef:
    name: server-lobby
  filterTags:
    pattern: '^(?P<timestamp>.*)Z$'
    extract: '$timestamp'
  policy:
    alphabetical:
      order: asc
