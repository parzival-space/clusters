apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: wg-easy
spec:
  entryPoints:
    - wireguard
  routes:
    - services:
        - name: wg-easy-wireguard
          port: udp-wireguard
