apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: wg-easy
spec:
  entryPoints:
    - wireguard
  routes:
    - match: Host(`*`)
      kind: Rule
      services:
        - name: wg-easy
          port: wireguard
