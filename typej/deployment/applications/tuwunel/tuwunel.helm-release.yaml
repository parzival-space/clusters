apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tuwunel
  namespace: tuwunel
spec:
  chart:
    spec:
      chart: tuwunel
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: tuwunel
      version: v1.10.1
  interval: 15m0s
  valuesFrom:
    - kind: ConfigMap
      name: tuwunel
      valuesKey: values.yaml
    - kind: Secret
      name: tuwunel
      valuesKey: values.yaml
